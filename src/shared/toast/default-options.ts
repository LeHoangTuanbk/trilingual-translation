import { ToastPosition } from "@chakra-ui/react";

export const DefaultMessages = {
  Error: "An error occurred",
  Success: "Success",
};

export const DefaultPosition = "top-right" as ToastPosition;
